# 求职保护模式

找工作期间，最怕的就是把**面试电话**当成骚扰电话挂断。为此，本项目新增「**求职保护模式**」。

---

## 开启后的行为变化

| 场景 | 普通模式 | 求职保护模式 |
|------|----------|--------------|
| 黑名单命中 | 自动挂断 | 自动挂断 |
| 白名单命中 | 不拦截，显示悬浮窗 | 不拦截，显示悬浮窗 |
| 被标记为骚扰/营销/诈骗 | 若开启「骚扰自动挂断」则挂断 | **不会自动挂断**，仅在悬浮窗提示 |
| 完全陌生的号码 | 可能挂断（若开启自动挂断）；若开启「陌生号静默」则静音不响铃 | **不挂断**，悬浮窗标红提醒「陌生号码，请留意」 |
| 固话反查到正规公司 | 正常显示 | 正常显示，可优先接听 |

> 注意：开启求职保护模式时，**「陌生号静默」会自动失效**（即使开关开着），确保面试/重要来电能正常响铃。

---

## 推荐配置（找工作期间）

1. 打开 **来电悬浮窗**
2. 打开 **求职保护模式**
3. 关闭或谨慎开启 **自动挂断**
   - 如果 still 想拦黑名单，可以保留自动挂断 + 开启「仅黑名单」
   - 这样只有明确被你拉黑过的号码才会挂断
4. 提前把可能的招聘公司电话/固话段加入 **白名单**
   - 例如：把 `010`、`021`、`0755` 等大城市的固话前缀加入白名单（会放行该前缀所有来电）
   - 或者把已知 HR 的手机号加入白名单
5. 来电时看一眼悬浮窗：
   - 显示「北京」「上海」「正规公司」→ 优先接
   - 显示「未知」「境外」「被多人标记骚扰」→ 谨慎接，但不自动挂断

---

## 求职结束后

等工作确定后，建议：

- 关闭「求职保护模式」
- 重新开启「骚扰自动挂断」
- 把白名单里不再需要的固话前缀删掉

---

## 技术实现

- 设置开关保存在 `SettingsDataStore` 的 `enable_job_hunt_mode` 中。
- `CallHandlerService` 在判断是否根据骚扰标记挂断时，会检查该开关；开启时跳过骚扰标记挂断逻辑。
- 陌生号码在悬浮窗中通过 `errorMessage` 显示提示语。

---

## 注意事项

- 求职保护模式**不是完全防骗**，只是降低误伤概率；遇到明确诈骗仍可在接听后手动挂断并加入黑名单。
- 建议在手机系统设置里关闭本应用的「勿扰模式绕过」等强提醒，避免夜间被骚扰电话吵醒；如果担心错过重要电话，可保留白名单。
